{
  "name": "grazyna-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "node -e \"require('fs').mkdirSync('dist',{recursive:true})\" && tsc",
    "start": "node dist/cluster-entry.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts"
  },
  "dependencies": {
    "@socket.io/redis-adapter": "^7.0.0",
    "fastify": "^4.0.0",
    "fastify-socket.io": "^5.0.0",
    "ioredis": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
